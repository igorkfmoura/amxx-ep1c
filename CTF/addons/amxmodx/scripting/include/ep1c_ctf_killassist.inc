#if defined _killassist_included
	#endinput
#endif

#define _killassist_included
#pragma library killassist

/**
 * Called when happens an assist
 *
 * @param iPlayer		Index of player
 */
forward onKillAssist(id)